請你以「50 人同時 WebSocket + TTS 壓測」為目標，
檢視目前的 ws_gateway_tts 實作是否準備好。

請聚焦以下檔案：
- ws_gateway_tts/server.py
- ws_gateway_tts/session.py
- ws_gateway_tts/protocol.py
- ws_gateway_tts/tts_engines/dummy.py

測試情境：
- 50 個 client 同時連線到 ws://localhost:9000/tts
- 每個 client 流程為：start → 多次 text_delta → text_end
- server 會持續回傳 audio_chunk，最後送出 tts_end
- duration 約 60 秒
- client 可能有慢連線（播放或接收變慢）

請完成以下任務（不要修改任何程式碼）：
1. 指出在「50 人同時連線」下，最可能出現的 3 個瓶頸或失敗模式
2. 每一點請說明：
   - 具體發生在什麼地方（檔案 / 邏輯）
   - 為什麼在 50 人時特別容易出問題
3. 對每一個問題，提出「最小可行的修補方向」（只描述，不實作）
4. 提供一個驗證計畫：
   - 該用哪些 benchmark 參數（baseline / mixed）
   - 要觀察哪些指標才能確認是否穩定

限制：
- 不要更改 WebSocket 協定
- 不要做架構重構
- 不要新增外部依賴

請用條列式輸出，並清楚分段。
