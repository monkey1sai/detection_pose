請根據前一個 50 人壓測檢視結果，
只實作「最小且必要」的穩定性修補，不要做其他改動。

只允許做以下兩類事情（如果你判斷有必要）：

1) 防止記憶體或 queue 無限制成長
   - 例如 send_queue 在 client 變慢時可能持續累積
   - 請使用既有的 backpressure / error 流程
   - 不要改 protocol，不要改 message type

2) 確保任務能乾淨結束
   - client 中斷或送出 text_end / error 時
   - sender_loop / synth_task 不應卡住或殘留

限制條件：
- WebSocket 協定完全不變
- message 格式完全不變
- DummyTtsEngine 行為不變
- 不做 refactor，不調整整體流程

請輸出：
- 簡短說明為什麼要改
- 對應的最小 diff（或明確描述要改哪幾行）
- 改完後，如何用 concurrency=50 驗證成功
