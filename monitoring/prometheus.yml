# ==================================================
# Prometheus 配置 - sglang 監控
# ==================================================

global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    monitor: 'sglang-monitor'

# -------------------- 告警規則 --------------------
rule_files:
  # - "alert_rules.yml"

# -------------------- 抓取配置 --------------------
scrape_configs:
  # sglang 服務指標
  - job_name: 'sglang'
    static_configs:
      - targets: ['sglang:8000']
    metrics_path: /metrics
    scrape_interval: 10s
    scrape_timeout: 10s

  # Nginx 指標 (需要 nginx-prometheus-exporter)
  # - job_name: 'nginx'
  #   static_configs:
  #     - targets: ['nginx-exporter:9113']

  # Prometheus 自我監控
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']
